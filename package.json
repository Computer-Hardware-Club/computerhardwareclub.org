{
  "name": "computerhardwareclub-org",
  "version": "1.0.0",
  "description": "Static website for the Computer Hardware Club at Oregon State University.",
  "scripts": {
    "test": "node --test",
    "build": "node build.mjs",
    "verify": "npm test && npm run build"
  },
  "private": true,
  "engines": {
    "node": ">=20"
  }
}
